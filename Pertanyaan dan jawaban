#Jawab Pertanyaan Berikut
1. Lakukan perubahan pada kode sesuai dengan keinginan anda, amati perubahannya adakah
   error ketika terjadi kesalahan penulisan tag?
2. Apa perbedaan dari tag <p> dengan tag <br>, berikan penjelasannya!
3. Apa perbedaan atribut title dan alt pada tag <img>, berikan penjelasannya!
4. Untuk mengatur ukuran gambar, digunakan atribut width dan height. Agar tampilan gambar
   proporsional sebaiknya kedua atribut tersebut diisi semua atau tidak? Berikan penjelasannya!
5. Pada link tambahkan atribut target dengan nilai atribut bervariasi ( _blank, _self, _top,
   _parent ), apa yang terjadi pada masing-masing nilai antribut tersebut?

#Jawaban
1. Tidak ada
2. Tag <p> digunakan untuk menandakan paragraf. Setiap kali menambahkan tag <p> di sekitar teks, itu akan memulai paragraf baru.
   Tag <br> adalah elemen inline yang digunakan untuk membuat baris baru tanpa memulai paragraf baru. Ini sering disebut sebagai tag "break line" atau "line break".
3. Atribut alt digunakan untuk menyediakan teks alternatif yang akan ditampilkan jika gambar tidak dapat dimuat atau dilihat oleh pengguna.
   Ini sangat penting untuk aksesibilitas, terutama bagi pengguna yang menggunakan pembaca layar (screen reader) atau memiliki keterbatasan penglihatan.
   
   Atribut title digunakan untuk menampilkan tooltip (teks yang muncul ketika pengguna mengarahkan kursor ke elemen) yang memberikan informasi tambahan tentang gambar.
   Ini lebih fokus pada pengalaman pengguna.
5. Untuk menampilkan gambar secara proporsional di sebuah halaman web, 
  sebaiknya tidak mengisi kedua atribut width dan height secara bersamaan jika ukuran gambar asli tidak diketahui.
  jika width dan height diisi keduanya:
     - Gambar akan dipaksa menyesuaikan ukuran sesuai dengan nilai yang diberikan, 
      yang dapat menyebabkan distorsi jika aspek rasio gambar asli berbeda.
  Jika hanya width atau height yang diisi:
    - Browser akan otomatis menghitung dimensi lainnya sesuai dengan rasio asli dari gambar tersebut, sehingga gambar tidak terdistorsi.

6. target="_blank":
     -Link akan dibuka di tab atau jendela browser baru.
   target="_self":
     -Ini adalah nilai default jika atribut target tidak ditentukan.
     -Link akan dibuka di jendela atau tab yang sama dengan halaman saat ini.
   target="_top":
     -Link akan dibuka di jendela utama browser, menggantikan semua frame jika ada.
     -Jika halaman tidak berada dalam frame, perilakunya sama dengan "_self".
   target="_parent":
     -Link akan dibuka di frame induk dari frame saat ini.
     -Jika tidak ada frame induk (halaman berada di level teratas), perilakunya sama dengan "_self".
